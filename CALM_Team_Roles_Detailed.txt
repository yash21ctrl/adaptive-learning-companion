👥 Team Roles & ResponsibilitiesCALM — Adaptive Learning CompanionHackNexus'26 — PS-08 | Detailed Role Breakdown📌 Read only your own section fully before starting. Check the 'Depends On' and 'Others Depend On You For' notes so you know exactly when to sync with teammates.🅰️  Person A — Backend, API & AI EngineOverall goal: build the entire Flask backend, the database, and all three layers of the AI system (classifier, verification, LLM narration).Phase 1 — Basic SetupSet up a Flask project with CORS enabledDesign the database schema: questions table (id, subject, topic, sub_topic, difficulty, text, answer, options), session_logs table (all tracked behavior fields + result per attempt)Build GET /get-next-question — returns the next question for a student based on their current subject/topic/sub-topic, never sends the answer fieldBuild POST /submit-answer — accepts the answer plus all tracked behavior data, checks correctness, saves the full record to the databasePhase 2 — Core Adaptive LogicBuild the sub-topic-targeted logic: when a student struggles, the next question comes from the SAME sub-topic at a lower difficulty, not a random topicBuild GET /get-results — returns a student's own lesson/sub-topic history and scores for the Results ScreenBuild the login-default logic: apply starting defaults based on the soft preference answers, but let real behavior override over timePhase 3 — The AI Engine (3 layers)LAYER 1 — Coordinate with the whole team to collect labeled training data (everyone plays through the app multiple times, varying behavior, self-labeling frustration)Train a Decision Tree classifier (scikit-learn) on retry_count, time_taken, tab_switches, mouse_idle_time, typing_pauses, used_visual_toggle -> predicts Low/Medium/High frustrationSave the trained model, load it in Flask, use it inside /submit-answerLAYER 2 — Build the verification pass: send the classifier's prediction + raw numbers to an LLM, ask it to sanity-check if the prediction matches the evidence (works in BOTH directions — catches over- and under-predictions), use the corrected value going forwardLAYER 3 — Build the LLM narration call: turn the final verified prediction into a natural sentence for Kip's tap-to-expand reasoning display (demo-only, Person C/D will call this from the frontend)Expose an endpoint like GET /get-kip-reasoning that returns Kip's plain-English explanation for the current session, for Person C to wire into Kip's UIPhase 4 — Reset Mission TriggerWhen High frustration is confirmed (post-verification), the backend should signal the frontend to show a Reset Mission BEFORE sending the easier next question — add a field like "trigger_reset_mission": true in the /submit-answer responseDepends onPerson C's questions.json (to test with real content); the whole team's participation in data collection for trainingOthers depend on you forEvery API endpoint — Person B and Person C cannot finish integration until your routes are live and stable🅱️  Person B — Full App FrontendOverall goal: build every screen in the app and all behavior tracking. This is the biggest visual build — you own the whole student experience.Phase 1 — Core ScreensLogin screen (with the soft optional preference question)Home/Dashboard screen (greeting, streak, gamification icons, motivational quote, Start Learning button)Learn Tab: Subject selector (Math/Science/English) -> Topic selector -> Sub-topic selectorLesson screen (short explainer + Skip button)Settings screenPhase 2 — The Question Screen (core of the app)Build the reusable Question Screen: progress bar, question text, answer input (typing or MCQ depending on question type), Submit buttonBuild the Visual Toggle: subject-aware — Math shows dots/number-line, Science shows an animated concept diagram, English shows highlighted/color-coded textBuild the tick-mark animation on correct answers, and the ladder-climbing animation on sub-topic completionBuild the Results Screen: list of attempted lessons, scores, Retry button per lessonPhase 3 — Behavior Tracking (critical for the AI engine)Tab switches: document.addEventListener('visibilitychange', ...)Mouse idle time: track mousemove events, measure the longest gap with no movementTyping pauses + backspace count: track on the answer input's keydown/keyup eventsTime taken: timestamp when question loads, timestamp when Submit is clickedBundle all of this into the POST /submit-answer call on every questionPhase 4 — Integration with KipReserve a fixed corner spot on the Question Screen for Kip (Person C builds Kip's component, you just need to leave the right container/props for it to plug into)When the backend signals trigger_reset_mission: true, pause the normal question flow and let Kip's Reset Mission component take over the screen briefly before continuingDepends onPerson A's API endpoints (to replace any placeholder/mock data); Person C's Kip component (to embed it)Others depend on you forThe actual usable app — Person C's Kip needs a screen to live in, Person D needs a working app to demo/screenshot for the PPT🅲  Person C — Questions, Lessons & KipOverall goal: write all learning content, AND build Kip — the character that brings the AI to life.Phase 1 — Content WritingWrite ~45 questions across Math, Science, and English (3 topics each, sub-topics within each, 5+ questions per sub-topic, Easy->Medium->Hard)Tag every question correctly: subject, topic, sub_topic, difficulty, answer, options (if MCQ) — accuracy here directly determines how well the AI's targeting worksWrite 2 short lessons per subject (10-15 second explainers, plain and simple language)Follow the exact schema Person A defines for questions.json / the databasePhase 2 — Kip's Visual StatesDesign/build Kip's 4 facial expression states: Calm, Curious/Concerned, Tired/Sad, Celebrating — as simple images or CSS-animated componentsBuild the state-switching logic: Kip's expression changes based on the frustration_level value returned from the backend after each questionBuild the tap-to-expand interaction: tapping Kip reveals the LLM-generated reasoning text from Person A's /get-kip-reasoning endpoint (demo mode only)Phase 3 — Kip's Reset MissionsBuild the Reset Mission pop-up/takeover screen that appears when the backend signals trigger_reset_mission: trueImplement the mission pool: silly personal questions, physical reset prompts, imagination prompts, breathing-beat animation — picked randomly each timeBuild Kip's warm reaction after a mission is completed ("Nice! Feeling refreshed?") before handing back control to the normal question flowMicro-game version (tap-the-circle, rock-paper-scissors) is a STRETCH GOAL — only build if time allows after everything else worksPhase 4 — TestingOnce Person A/B have a working build, test the full app as a real student would — try every question type, use the visual toggle, deliberately trigger frustration, and report bugs back to the teamHelp the team collect labeled training data by playing through varied test sessionsDepends onPerson A's schema for questions (to format content correctly); Person A's frustration_level and reasoning endpoints (for Kip to react to)Others depend on you forPerson B needs your Kip component to embed in the Question Screen; Person A needs your questions.json to test the backend with real content🅳  Person D — PPT, Pitch & DocumentationOverall goal: build the Round 1 submission (PPT + Abstract) and prepare the team for Q&A.Phase 1 — Round 1 DeliverablesWrite the Project AbstractBuild the 8-9 slide PPT: Problem Statement, Proposed Solution, Technical Implementation Approach, System/Architecture Diagram, Expected Impact, Business Model/Scalability, Team Information, ConclusionDraw the architecture diagram: Frontend, Backend, AI Layer (3 parts), Database — with arrows showing data flow, matching the master reference docPhase 2 — The NarrativeWrite the impact narrative — grounded in real ADHD/dyscalculia struggles, explaining how each feature (visual toggle, adaptive difficulty, Kip, reset missions) responds to a specific real problemExplain the AI honestly and specifically: a trained classifier + a verification layer + an LLM narration layer — avoid vague buzzwordsEmphasize the 'normal app, invisible support layer' framing — this app should never sound like it treats students as disabledPhase 3 — Q&A PreparationPrepare answers for likely questions: 'How does your AI actually work?', 'How did you get training data?', 'What if the model predicts wrong?', 'Why no camera?', 'Why no parent dashboard?'Get screenshots/short clips from Person B's working app once available, to strengthen the PPT with real visuals (not required for Round 1, but strengthens Technical Feasibility scoring)Phase 4 — Later (Round 2 prep)Coordinate what the Round 2 demo video will show once the app is built — script the sequence (calm session -> struggle -> Kip reacts -> reset mission -> results screen -> Kip's reasoning shown for judges)Depends onUnderstanding from the master reference doc (already complete); later, screenshots from Person B's working appOthers depend on you forThe actual Round 1 submission — without this, nothing else matters regardless of how good the build is✅ Everyone can start today. Person C and Person D have zero blocking dependencies to begin — Person A and Person B should sync briefly on the API contract before diving in.